# Local environment variables for docker-compose and Spring Boot (development)
DB_NAME=saas_app_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_PORT=5432
DB_HOST=saas-db
# Optionally set Spring active profile to 'dev' when running locally
SPRING_PROFILES_ACTIVE=dev

